### Para executar o projeto siga os passos abaixo:

```bash
mvn clean compile
mvn exec:java -Dexec.mainClass="br.uff.sti.AppLombok"
```

A string CSV deve ser informada no terminal. Para terminar a leitura de dados, deixe uma linha em branco (somente com o caracter "\n").
